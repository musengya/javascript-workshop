/* eslint-disable no-undef */
"use strict";

const firstName = "Irene";
const lastName = "Anto";

if (firstName === "Irene") {
    if (lastName === "Mutua") {
        document.write("Welcome Irene Mutua!"); // eslint-disable-line
    } else {
        document.write("welcome Irene someone");
    }
}
